<% if users.any? %>
  <ul class="list-unstyled">
    <% users.each do |user| %>

          <div>
            <%= user.name %>
          </div>
          <div>
            <p>#%= link_to 'ユーザー情報', user_path(user) %></p>
          </div>
        </div>
      </li>
    <% end %>
  </ul>
  <%= paginate users %>
<% end %>